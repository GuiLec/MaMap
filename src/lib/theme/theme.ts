const colors = {
  primary: 'red',
  activeBlur: 'rgba(85, 219, 18, 0.4)',
  inactiveBlur: 'rgba(219, 15, 15, 0.4)',
  white: '#ffffff',
  black: '#000000',
};

const gridUnit = 5;

const fontSizes = {
  small: 12,
};

export const theme = {
  colors,
  gridUnit,
  fontSizes,
};
